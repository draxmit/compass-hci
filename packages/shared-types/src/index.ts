// Shared types across the Compass monorepo.
// Populated by T2 (Account, Workspace, User) and T6 (Transaction, Category).
// TODO(T2): export Account, Workspace, User types
// TODO(T6): export Transaction, Category, Split types
export {};
